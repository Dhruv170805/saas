{
    # Global options
    email your-email@example.com
}

# Wildcard and main domain handling
# Caddy handles Let's Encrypt certificates automatically
*.nexuspos.local, nexuspos.local {
    reverse_proxy nginx:80
}

# Optional: Add specific tenant domains if they use custom domains in the future
# your-custom-domain.com {
#     reverse_proxy nginx:80
# }
